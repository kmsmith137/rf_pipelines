#ifndef _RF_PIPELINES_HPP
#define _RF_PIPELINES_HPP

#if (__cplusplus < 201103) && !defined(__GXX_EXPERIMENTAL_CXX0X__)
#error "This source file needs to be compiled with C++11 support (g++ -std=c++11)"
#endif


// Definitions of core classes (ring_buffer, pipeline_object, etc.)
#include "rf_pipelines_base_classes.hpp"

// For a list of all pipeline_objects defined in rf_pipelines, see here!
#include "rf_pipelines_inventory.hpp"


#endif // _RF_PIPELINES_HPP
